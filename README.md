# ShowTrack

Personal TV show tracker (PWA). Data lives in your browser (IndexedDB); episode data from the free TVmaze API. Includes an importer for the TV Time GDPR export ZIP.

No build step — static files. Open index.html via any web server.
